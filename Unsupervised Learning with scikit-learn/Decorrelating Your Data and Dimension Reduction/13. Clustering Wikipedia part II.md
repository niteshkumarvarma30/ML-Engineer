# Exercise

## Clustering Wikipedia part II

### Concept: Executing the NLP Pipeline

It is now time to put your pipeline from the previous exercise to work! 

You have built an elegant pipeline that first uses `TruncatedSVD` to compress word-frequency data, and then immediately feeds that compressed data into a `KMeans` clustering algorithm.

In this exercise, you will execute that pipeline on a massive array called `articles` (which contains tf-idf word-frequencies of popular Wikipedia articles). First, you will `.fit()` the pipeline so the algorithms can learn the patterns. Then, you will `.predict()` the cluster labels for each article.

To see if the algorithm worked, you will use `pandas` to align the numeric cluster labels with the actual English `titles` of the articles and sort them. If the clustering was successful, articles about similar topics (like musicians, scientists, or athletes) will share the same cluster number!

*(Note: The `pipeline` from the previous exercise has been pre-loaded for you.)*

### Instructions

* Import `pandas` as `pd`.
* Fit the pipeline to the word-frequency array `articles`.
* Predict the cluster labels.
* Align the cluster labels with the list `titles` of article titles by creating a DataFrame `df` with `labels` and `titles` as columns. This has been done for you.
* Use the `.sort_values()` method of `df` to sort the DataFrame by the `'label'` column, and print the result.
* Hit submit and take a moment to investigate your amazing clustering of Wikipedia pages!

---

### Starter Code

```python
# Import pandas
____

# Fit the pipeline to articles
____

# Calculate the cluster labels: labels
labels = ____

# Create a DataFrame aligning labels and titles: df
df = pd.DataFrame({'label': labels, 'article': titles})

# Display df sorted by cluster label
print(____)
```

---

### Solution

```python
# Import pandas
import pandas as pd

# Fit the pipeline to articles
pipeline.fit(articles)

# Calculate the cluster labels: labels
labels = pipeline.predict(articles)

# Create a DataFrame aligning labels and titles: df
df = pd.DataFrame({'label': labels, 'article': titles})

# Display df sorted by cluster label
print(df.sort_values('label'))
```
