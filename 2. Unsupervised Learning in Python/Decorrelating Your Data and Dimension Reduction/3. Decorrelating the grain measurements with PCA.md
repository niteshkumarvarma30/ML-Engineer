# Exercise

## Decorrelating the grain measurements with PCA

### Concept: The Power of PCA De-correlation

In the previous exercise, you proved that the width and length of the grain samples were highly correlated. Now, you will see the true power of PCA's first step. 

You'll use PCA to **decorrelate** these measurements. You will learn the transformation and apply it simultaneously using the `.fit_transform()` method. Then, you'll plot the new "PCA features" and measure their Pearson correlation. 

If the PCA math worked correctly, it will have shifted and rotated the data so perfectly that the new X and Y coordinates (the PCA features) will have a Pearson correlation of exactly $0$!

### Instructions

* Import `PCA` from `sklearn.decomposition`.
* Create an instance of `PCA` called `model`.
* Use the `.fit_transform()` method of `model` to apply the PCA transformation to `grains`. Assign the result to `pca_features`.
* The subsequent code to extract, plot, and compute the Pearson correlation of the first two columns of `pca_features` has been written for you, so just review it to see how the proof works!

---

### Starter Code

```python
# Import PCA
____
import matplotlib.pyplot as plt
from scipy.stats import pearsonr

# Create PCA instance: model
model = ____

# Apply the fit_transform method of model to grains: pca_features
pca_features = ____

# Assign 0th column of pca_features: xs
xs = pca_features[:,0]

# Assign 1st column of pca_features: ys
ys = pca_features[:,1]

# Scatter plot xs vs ys
plt.scatter(xs, ys)
plt.axis('equal')
plt.show()

# Calculate the Pearson correlation of xs and ys
correlation, pvalue = pearsonr(xs, ys)

# Display the correlation
print(correlation)
```

---

### Solution

```python
# Import PCA
from sklearn.decomposition import PCA
import matplotlib.pyplot as plt
from scipy.stats import pearsonr

# Create PCA instance: model
model = PCA()

# Apply the fit_transform method of model to grains: pca_features
pca_features = model.fit_transform(grains)

# Assign 0th column of pca_features: xs
xs = pca_features[:,0]

# Assign 1st column of pca_features: ys
ys = pca_features[:,1]

# Scatter plot xs vs ys
plt.scatter(xs, ys)
plt.axis('equal')
plt.show()

# Calculate the Pearson correlation of xs and ys
correlation, pvalue = pearsonr(xs, ys)

# Display the correlation
print(correlation)
```
