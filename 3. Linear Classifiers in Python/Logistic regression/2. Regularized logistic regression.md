# Exercise

## Regularized logistic regression

### Concept: Tuning the C Hyperparameter (L2 Regularization)
In Chapter 1, you used logistic regression on the handwritten digits data set. Here, we'll explore the effect of L2 regularization by testing different values of the `C` hyperparameter.

Remember that `C` is the *inverse* of regularization strength. A small `C` means strong regularization (small coefficients), while a large `C` means weak regularization (large coefficients).

The handwritten digits dataset is already loaded, split, and stored in the variables `X_train`, `y_train`, `X_valid`, and `y_valid`. The variables `train_errs` and `valid_errs` are already initialized as empty lists.

---

### Instructions

1. Loop over the different values of `C_value`, creating and fitting a `LogisticRegression` model each time.
2. Save the error on the training set and the validation set for each model. (Note: Error is just `1.0 - accuracy_score`).
3. Create a plot of the training and testing error as a function of the regularization parameter, `C`.
4. Looking at the plot, what's the best value of `C`?

---

### Starter Code (Question)

```python
import matplotlib.pyplot as plt
from sklearn.linear_model import LogisticRegression

# Train and validation errors initialized as empty list
train_errs = list()
valid_errs = list()

C_values = [0.001, 0.01, 0.1, 1, 10, 100, 1000]

# Loop over values of C_value
for C_value in C_values:
    # Create LogisticRegression object and fit
    lr = LogisticRegression(C=C_value)
    lr.fit(____)
    
    # Evaluate error rates and append to lists
    train_errs.append( 1.0 - lr.score(____) )
    valid_errs.append( 1.0 - lr.score(____) )
    
# Plot results
plt.semilogx(C_values, train_errs, C_values, valid_errs)
plt.legend(("train", "validation"))
plt.show()
```

---

### Solution

```python
import matplotlib.pyplot as plt
from sklearn.linear_model import LogisticRegression

# Train and validation errors initialized as empty list
train_errs = list()
valid_errs = list()

C_values = [0.001, 0.01, 0.1, 1, 10, 100, 1000]

# Loop over values of C_value
for C_value in C_values:
    # Create LogisticRegression object and fit
    lr = LogisticRegression(C=C_value)
    lr.fit(X_train, y_train)
    
    # Evaluate error rates and append to lists
    train_errs.append( 1.0 - lr.score(X_train, y_train) )
    valid_errs.append( 1.0 - lr.score(X_valid, y_valid) )
    
# Plot results
plt.semilogx(C_values, train_errs, C_values, valid_errs)
plt.legend(("train", "validation"))
plt.show()
```

**Takeaway:** When you plot this, you will see a U-shaped curve for the validation error! If `C` is too small (too much regularization), the model is underfit and both errors are high. If `C` is too large (too little regularization), the training error drops to zero but the validation error starts creeping back up due to overfitting! The optimal `C` is right at the bottom of that U-curve.
