# Exercise

## Logistic regression and feature selection

### Concept: Using L1 Regularization to delete useless features
In this exercise we'll perform feature selection on the movie review sentiment data set using **L1 regularization**. 

Unlike L2 (which just shrinks coefficients), L1 pushes the coefficients of useless words all the way down to exactly `0.0`. This completely removes them from the model, acting as a built-in feature selector!

We'll search for the best value of `C` using scikit-learn's `GridSearchCV()`. The features and targets are already loaded for you in `X_train` and `y_train`.

---

### Instructions

1. Instantiate a logistic regression object that uses L1 regularization. (Remember to set the solver to `liblinear`!)
2. Find the value of `C` that minimizes cross-validation error using `GridSearchCV`.
3. Print out the number of selected features for this optimal value of `C`.

---

### Starter Code (Question)

```python
from sklearn.linear_model import LogisticRegression
from sklearn.model_selection import GridSearchCV
import numpy as np

# Specify L1 regularization
lr = LogisticRegression(solver='liblinear', ____)

# Instantiate the GridSearchCV object and run the search
searcher = GridSearchCV(lr, {'C':[0.001, 0.01, 0.1, 1, 10]})
searcher.fit(X_train, y_train)

# Report the best parameters
print("Best CV params", searcher.best_params_)

# Find the number of nonzero coefficients (selected features)
best_lr = searcher.best_estimator_
coefs = best_lr.____
print("Total number of features:", coefs.size)
print("Number of selected features:", np.count_nonzero(____))
```

---

### Solution

```python
from sklearn.linear_model import LogisticRegression
from sklearn.model_selection import GridSearchCV
import numpy as np

# Specify L1 regularization
lr = LogisticRegression(solver='liblinear', penalty='l1')

# Instantiate the GridSearchCV object and run the search
searcher = GridSearchCV(lr, {'C':[0.001, 0.01, 0.1, 1, 10]})
searcher.fit(X_train, y_train)

# Report the best parameters
print("Best CV params", searcher.best_params_)

# Find the number of nonzero coefficients (selected features)
best_lr = searcher.best_estimator_
coefs = best_lr.coef_
print("Total number of features:", coefs.size)
print("Number of selected features:", np.count_nonzero(coefs))
```

**Takeaway:** `GridSearchCV` will find the best `C` value that balances accuracy with the strictness of the L1 penalty. When you run `np.count_nonzero(coefs)`, you will see that out of the thousands of words in the dataset, the L1 model cleverly set the vast majority of their coefficients to `0.0`, leaving only the most important words behind!
