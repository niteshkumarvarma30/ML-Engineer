# Exercise

## RandomSearchCV inputs

Let's test your knowledge of how `RandomizedSearchCV` differs from `GridSearchCV`.

You can check the documentation on Scikit Learn's website to compare these two functions.

Which of these parameters is *only* for a `RandomizedSearchCV`?

### Possible answers

*   `param_grid`
*   `n_jobs`
*   `best_estimator_`
*   `n_iter`

**Solution:**

**`n_iter`**
