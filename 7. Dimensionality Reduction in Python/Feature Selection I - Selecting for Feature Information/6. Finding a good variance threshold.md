# Exercise

## Finding a good variance threshold

### Concept: Visualizing Variance
You'll be working on a slightly modified subsample of the ANSUR dataset with just head measurements pre-loaded as `head_df`.

### Instructions

**Step 1:**
*   Create a boxplot on `head_df`.

```python
# Create the boxplot
head_df.____

plt.show()
```

**Step 2:**
*   Normalize the data by dividing the DataFrame with its mean values.

```python
# Normalize the data
normalized_df = ____ / ____

normalized_df.boxplot()
plt.show()
```

**Step 3:**
*   Print the variances of the normalized data.

```python
# Normalize the data
normalized_df = head_df / head_df.mean()

# Print the variances of the normalized data
print(____.____)
```

**Step 4:**
*   **Question:** Inspect the printed variances. If you want to remove the 2 very low variance features, what would be a good variance threshold?
    *   1.5e-03
    *   1.5e-02
    *   **1.0e-03** *(Correct)*
    *   1.0e-02

*(The printed output shows `n_hairs` with 1.003e-08 and `measurement_error` with 0.000e+00. A threshold of `1.0e-03` is higher than both of these, but lower than all the valid head measurements which are in the `e-03` range above 1.0!)*

### Solution

**Step 1 Solution:**
```python
# Create the boxplot
head_df.boxplot()

plt.show()
```

**Step 2 Solution:**
```python
# Normalize the data
normalized_df = head_df / head_df.mean()

normalized_df.boxplot()
plt.show()
```

**Step 3 Solution:**
```python
# Normalize the data
normalized_df = head_df / head_df.mean()

# Print the variances of the normalized data
print(normalized_df.var())
```
