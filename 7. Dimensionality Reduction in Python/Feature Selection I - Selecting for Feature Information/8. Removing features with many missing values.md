# Exercise

## Removing features with many missing values

### Concept: Identifying Missing Data
You'll apply feature selection on the Boston Public Schools dataset which has been pre-loaded as `school_df`. Calculate the missing value ratio per feature and then create a mask to remove features with many missing values.

### Instructions

**Step 1:**
*   **Question:** In what range lies the highest ratio of missing values for a single feature in the dataset?
    *   **Between 0.9 and 1.0.** *(Correct)*
    *   Between 0.8 and 0.9.
    *   Between 0.7 and 0.8.
    *   Between 0.6 and 0.7.

*(You can verify this by checking `school_df.isna().sum() / len(school_df)` in the console, which will reveal at least one column is missing over 90% of its data!)*

**Step 2:**
*   Create a boolean mask on whether each feature has less than 50% missing values.
*   Apply the mask to `school_df` to select columns without many missing values.

```python
# Create a boolean mask on whether each feature less than 50% missing values.
mask = school_df.____.____ / len(school_df) < 0.5

# Create a reduced dataset by applying the mask
reduced_df = ____

print(school_df.shape)
print(reduced_df.shape)
```

### Solution

**Step 2 Solution:**
```python
# Create a boolean mask on whether each feature less than 50% missing values.
mask = school_df.isna().sum() / len(school_df) < 0.5

# Create a reduced dataset by applying the mask
reduced_df = school_df.loc[:, mask]

print(school_df.shape)
print(reduced_df.shape)
```
