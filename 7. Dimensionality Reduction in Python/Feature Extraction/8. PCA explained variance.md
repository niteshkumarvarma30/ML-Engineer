# Exercise

## PCA explained variance

You'll be inspecting the variance explained by the different principal components of the `pca` instance you created in the previous exercise.

### Instructions

**Step 1:**
*   Print the explained variance ratio per principal component.

```python
# Inspect the explained variance ratio per component
print(____)
```

**Step 2:**
*   **Question:** How much of the variance is explained by the 4th principal component?
    *   About 3.03%
    *   About 3.77%
    *   About 6.8%
    *   About 61.45%

**Step 3:**
*   Calculate the cumulative sum of the explained variance ratio using a method of `pca.explained_variance_ratio_`.

```python
# Print the cumulative sum of the explained variance ratio
print(pca.explained_variance_ratio_____)
```

**Step 4:**
*   **Question:** What's the lowest number of principal components you should keep if you don't want to lose more than 10% of explained variance during dimensionality reduction?
    *   2 principal components
    *   3 principal components
    *   4 principal components
    *   5 principal components

### Solution

**Step 1 Solution:**
```python
# Inspect the explained variance ratio per component
print(pca.explained_variance_ratio_)
```

**Step 2 Solution:**
*   **About 3.77%**

**Step 3 Solution:**
```python
# Print the cumulative sum of the explained variance ratio
print(pca.explained_variance_ratio_.cumsum())
```

**Step 4 Solution:**
*   **4 principal components**
